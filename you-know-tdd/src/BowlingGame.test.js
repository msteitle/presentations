import { Game } from './BowlingGame';

describe('BowlingGame', () => {
  it.skip('returns a score of 0 for a gutter game (no bumpers)', () => false);

  it.skip('returns a score of 20 when all 1s are rolled (bumpers out)', () => false);

  it.skip('returns the correct score when one spare is rolled', () => false);

  it.skip('returns the correct score when one strike is rolled', () => false);

  it.skip('returns the correct score when a perfect game is played', () => false);
});